define(["lib/domain", "lib/formatters", "app/domain/model", "i18n!app/domain/nls/resources"
], function (domain, formatters, metadata, resources) {
	"use strict";

	domain.localize(metadata, resources);

	var model = domain.buildModel(metadata);

	return model;
});