{
    "directory": "vendor_components"
<% if (useCrocProxy) {  %>
    ,"proxy": "http://proxy.croc.ru:8000",
    "https-proxy": "http://proxy.croc.ru:8000"
<% } %>
<% if (useLocalBowerrc) { %>
<% if (useBowerArt) { %>
    ,"resolvers": [ "bower-art-resolver" ]
<% } %>
    ,"registry": "<%= bowerRegistryUrl %>"
<% } %>
}